const Calendars = require('../calendars/model.calendars')

module.exports ={
	addCalendar:function(req, res){
		console.log('addCalendar body', req.body)
		//add the calendar id to the database here
	}
}